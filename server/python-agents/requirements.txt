# NemoClaw God-Brain Architecture Dependencies
# Run `pip install -r requirements.txt` to initialize the local container

fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.2
requests==2.31.0
python-dotenv==1.0.0

# Future Phase Requirements natively supported by the Sandbox
# langchain-google-genai
# pipecat-ai
# twilio
